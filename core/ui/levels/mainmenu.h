#ifndef MAINMENU_H
#define MAINMENU_H

#include <ncurses.h>

typedef struct gameInterfaceTag gameInterface;
typedef struct gameLevelTag gameLevel;
typedef struct gameWindowTag gameWindow;

void mainMenu(gameInterface *);

#endif
